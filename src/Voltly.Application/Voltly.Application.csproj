<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>

  <ItemGroup>
    <!-- ORMs -->
    <PackageVersion Include="Microsoft.EntityFrameworkCore" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Design" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Proxies" />
    <PackageVersion Include="Oracle.EntityFrameworkCore" />

    <!-- CQRS / Mapping -->
    <PackageVersion Include="MediatR" />
    <PackageVersion Include="Mapster" />
    <PackageVersion Include="Mapster.SourceGenerator" />

    <!-- Infra / Helpers -->
    <PackageVersion Include="Scrutor" />
    <PackageVersion Include="FluentValidation" />
    <PackageVersion Include="Swashbuckle.AspNetCore" />
    <PackageVersion Include="LazyCache" />

    <!-- Testes -->
    <PackageVersion Include="xunit" />
    <PackageVersion Include="xunit.runner.visualstudio" />
    <PackageVersion Include="FluentAssertions" />
    <PackageVersion Include="Testcontainers" />
  </ItemGroup>
</Project>